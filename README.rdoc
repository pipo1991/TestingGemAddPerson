== README
This a simple app for testing the gem 'person-add'

You need before deploying the app to 

  bundle
  
all the gems to update Gemfile.lock

And run 

  rake db:migrate
  
to migrate database

